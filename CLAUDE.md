# CLAUDE.md

This is a static HTML personal website hosted on GitHub Pages.

There is no build step, no tests, and no package manager.

The main file is `index.html` in the root. Edit it directly.
Images are in `about_me/images/`. CSS is in `style.css`.